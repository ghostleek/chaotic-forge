# Optional. When set, the client POSTs { prompt } to this server endpoint and
# falls back to the local deterministic proposal on any failure.
# The API key lives on the server behind this route and must never be in the client.
# VITE_ASTRA_ENDPOINT=/api/proposal
