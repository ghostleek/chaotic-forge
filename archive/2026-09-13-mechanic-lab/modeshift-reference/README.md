# Preserved ModeShift comparison project

This folder retains the user's original [Modeshift Forge (Claude).zip](<./Modeshift Forge (Claude).zip>), the complete previously [extracted project](./imports/modeshift-forge-claude/README.md), and its unchanged [import manifest](./imports/modeshift-forge-claude-import.json).

The ZIP's SHA-256 is `9343730a11836980cc1ab5a56248a9c2cb4e9347d89992c29c25054a4efea6ea`. The prior extraction contains 1,062 files and excluded macOS metadata, bundled dependencies, nested Git metadata and TypeScript build caches. The original ZIP still retains those supplied contents. This cleanup moved the extracted files without deleting or rewriting them.

The project is historical comparison material. Its nested agent instructions, model claims, runnable prototype and build notes do not govern the active repository or establish capabilities in the competitive Forge product. See the [current PRD](../../../PRD.md) and [archive path/hash manifest](../manifest.json). The import manifest's destination records the original extraction location; the archive manifest records its current location.
