# PC-12 follow-up: shuffled hazards, hovering enemies and meteor deaths

User decision: randomize independent pterodactyl/spike placement and increase their density; flying birds hover one grid higher/lower; meteor waves occur at1500 score then every1000; meteor hits kill instantly; three skeleton sprites cover all death cases at the Dino's pre-hit size.

Source: existing user-selected Dino and pterodactyl references. Forge interpretation: retain a short three-object learning opening, then seeded independent weighted hazard choices and random gaps. Each restart requests a fresh browser-crypto seed; identical initial seeds/inputs replay identically. Gaps tighten with run time while speed remains uncapped. Flight oscillates between24 and104px altitude, ±40px around64, without lateral hovering offsets. Collision sweeps account for vertical movement as well as scrolling. Some high birds can be passed underneath.

Meteor waves contain2–4 meteors with random horizontal positions, altitudes and fall rates. Milestones are1500,2500,3500,etc., including score jumps from meat/stomps. Staggered60+ tick warnings precede diagonal falling paths. Meteors ignore damage protection and remaining lives, but the beam destroys objects intersecting its actual visible band. Flight above that band survives. Warning/timer state freezes on pause. All fatal collisions capture growth before damage shrinks the Dino; the renderer selects one of three original flat native-Canvas skeleton patterns. No raster generation or editing, new dependencies, network calls or model calls are used.

Observable outcome: replayable, varied local runs with flying hazards, milestone meteor waves and size-correct bone death poses. Human accountable owner: Kahhow; shared-runtime owner/acceptance Lance. Implementer Codex; independent reviewer review_dino. No new GitHub issue/PR verified. Exact base:51e6419da1e19daa5d991b61bc35adc0e1a1a737 (live Site21). Allowed files: optional Dino state/physics, progressive wrapper, shared renderer, demo/modal presentation and focused tests plus this handoff. Retained V1/V2 creation/replay defaults, party source bytes/hashes, legacy G1–G4 and provenance contracts are preserved. No auth, storage, migrations or API changes. Optional assets/dependencies: none.

Selected skills: Sites building for the existing project; React best-practices for UI edits; existing Playwright Worker harness for browser verification (agent-browser CLI unavailable). Native Canvas skeleton drawing follows the accepted flat Dino rendering style. Timebox: current implementation session. Rollback/cut: revert this scoped patch or restore Site21, preserving stored data.

Acceptance checks: deterministic same-seed layouts and different-seed variation; consecutive bird/bird and spike/spike choices; rising encounter frequency; hover extrema and swept contacts; exact repeated meteor milestones with warning, random positions, instant fatal collision through protection for all sizes; fatal-growth preservation for meteor/spike/bird; matching three skeleton renderings; UI pause/restart, uncapped speed, beam and full local golden flow. Run QUALITY.md lint/build/full tests, independent review, then post-review lint/build/focused checks. Deployment remains separate until requested.

Final verification: full npm test passed221/221 unit tests and58/60 browser checks; the two failures sampled identical zero-stride poses after the course timing changed. Corrected the animation check to compare243/249 ticks. Rebuilt after aligning drawn player pixels to whole coordinates; all24 Dino desktop/mobile browser checks then passed. Lint and36 focused Dino tests passed. Independent review found no blockers in seeded spawning, vertical sweeps, meteor lethality, milestones, retained defaults and fatal-growth capture; review did not independently rerun tests or assess broad multi-seed fairness.

Additional user decision: three supplied fossil/skull/skeleton reference images should improve the death sprites. Original native shapes now use stage-specific skull proportions, eye/nasal sockets, toothed jaws, curved ribs, segmented tails and jointed legs, with pale bone fill and dark edges. No stock image pixels or watermarks are copied. Independent review found no rendering-code blockers. Lint/build and36 focused tests passed; the final four desktop/mobile checks for all three poses and actual meteor-wave/death flow passed. Visually inspected the three-pose screenshot at game scale. An initial browser retry was blocked by automatic approval review due to a usage limit; the user requested retry, and the subsequent approved run completed successfully. No deployment performed.
