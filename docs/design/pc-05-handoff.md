# PC-05 — pixel instruction game

Packet [PC-05 / issue #34](https://github.com/ghostleek/chaotic-forge/issues/34), extending the user-approved PC-04 room stack. Accountable human owner: Kahhow (@ghostleek). Implementer: Codex. Reviewer: Lance (@Leoendithas), acceptance pending; a bounded independent source review by Codex identified and resolved manifest-binding, dormant-rule-mapping and legacy archive resolver issues. This is not the complete QUALITY.md review.

Base: merged [PC-04 PR #46](https://github.com/ghostleek/chaotic-forge/pull/46), `1dbccea77d4d2707328c704f1d20be8aabc3b2ac`, including PC-02/03/06. The current branch `codex/pc-05-pixel-arcade` retains earlier PC-05 renderer work and adds the explicitly requested pixel pivot. No new PC-05 PR has been created yet.

Observable outcome: two or three people submit one instruction each, generate a pixel game, review the resulting rules and scoring, acknowledge readiness, and play a synchronized 60-second challenge in separate arenas. Submitted captured inputs are replayed by the server to determine points, hits and ranks.

User decision: simple typography-heavy UI, code-based pixel assets only, optional starter cards, two players sufficient with a third optional, no mixed hand or swap feature. Reuse existing room/polling/capability, input-capture, clock, result, edit-right and archive infrastructure. New model output is a bounded executable configuration interpreted by a retained engine; it is not arbitrary generated source or complete [PC-09 / issue #38](https://github.com/ghostleek/chaotic-forge/issues/38).

Files: `components/party-forge/`, `lib/party-forge/`, new D1 generation-job schema/migration and packet/provenance docs. Kahhow owns UI integration; Lance remains accountable for shared contracts, runtime, scoring and storage review. Root Codex integrates shared changes for this authorized pivot. Hosting identity and existing legacy executable resources are preserved.

Contracts: each original instruction is retained as User decision / user-concept Source; its generated interpretation and active fields are retained separately. Real OpenAI Responses generation uses `gpt-6-astra` and persists returned model/response provenance. No preset fallback masquerades as generation. Recipes and exact retained executable hashes support replay without regeneration. Same start, seed and scoring rules apply across each frozen roster. The UI never produces authoritative scores.

Bounds: Snake, Invaders or Bounce movement plus supported food, wrapping, enemies, shooting, ricochet and scoring parameters. Unsupported instructions fail explicitly. Three generation attempts per room, five total instruction cards, no arbitrary same-origin generated code. OpenAI key stays server-side in Sites secrets, outside source and archives. Existing Kitchen runtime and G1–G4 remain available. Saved pixel replacement of inherited cards is not implemented; retain immutable replay rather than silently generating a different game.

Skills: Next.js and installed client/server guide for boundaries; React component guidance; browser/verification guidance for requested local test; OpenAI docs for structured generation; Sites building/hosting and persistence guidance for existing Worker/D1 deployment. Existing Playwright/Chrome provides browser verification. No additional raster generator or asset dependency.

Validation: fresh lint, TypeScript and production build pass with two-player support and the submission retry fix. A real two-browser run generated Wraparound Snake, started both arenas and accepted one captured score; simultaneous submission exposed a stale-revision rejection in the second browser. Known rejected submissions now retry the same captured attempt at most twice, while unknown outcomes preserve the original command identity. Bounded independent review found no blocker in this fix. The repeated two-browser golden flow passed with no browser errors: real generation, readiness, synchronized play, complete input replay and server results visible in both browsers. Both scored 10 points / 0 hits, correctly shown as equal rank. This is local evidence, not hosted multi-device proof. Full suites are intentionally paused by the user. Before merge, run exact QUALITY.md gates: lint, build, npm test, test:party, complete independent diff review, then second lint/build/focused checks after fixes. No full regression or hosted multi-device proof is claimed.

Timebox: urgent local demonstration and requested GPT Sites publish. Cut decorative work and unsupported rule combinations first. Rollback: restore prior Sites publication and revert the incremental pixel changes, preserving stored/retained old runtime resources. No merge authorized.

Deployment source is a snapshot of project commit `76f1caf3d7d7e0087254df0a2e364de1378ac7d2` on the existing Sites history. The unused 94 MB historical reference ZIP is omitted solely from this deployment checkout because Sites rejected its size; it remains unchanged in the project/GitHub history. Runtime source and legacy behaviors are retained.

## Generation failure fix — 13 September 2026

Live D1 recorded an old generic output-limit failure for the instruction pair “tap tap revenge” and “sound turns off every 5 seconds.” The original code used that message for every non-completed model status, so the provider's precise incomplete reason was not retained. The current bounded runtime supports neither rhythm lanes nor audio. A mandatory recipe even for unsupported requests encouraged unnecessary output.

Unsupported requests can now return a null recipe with a concise explanation; supported requests still require complete validated rules. Output budget is 6,000 tokens. Provider status, incomplete reason, response ID and output-token count are retained for future failures without storing prompts or credentials in logs. Only the legacy known terminal failure can use a fresh policy-specific cache key; completed and uncertain jobs remain deduplicated, and the three-job room cap remains enforced.

Fresh live request checks: the exact reported unsupported pair returned a correct concise explanation with null recipe in 4 seconds (49 output tokens); the two-card Snake demo returned supported wrapping/food/scoring rules in 5 seconds (234 output tokens). Three focused regression checks passed. Full gameplay was not rerun because the retained executable did not change. Previous full local two-player golden flow remains the gameplay evidence; this is not a complete QUALITY.md pass or PC-09 acceptance.

## Reference meshing fix — 13 September 2026

User decision: a pair of game names asks Forge to combine recognizable mechanics, not reproduce both original control systems literally. The live failed room contained exactly “space invaders” and “snake”; the model rejected their different movement modes. The prompt now decomposes references, chooses one movement system and layers the other references' distinguishing mechanics. It explicitly qualifies Snake + Space Invaders in either order as snake movement/growth plus descending aliens and directional auto-fire. Explicit modifiers remain authoritative; unavailable executable capabilities still receive a clear explanation.

The specific old terminal movement-conflict failure can use one policy-specific recovery key while retaining original attempts, completed/uncertain cache entries and the three-job room cap. Existing output-budget recovery keys are preserved. The engine, manifest/replay format and scoring code are unchanged.

Checks: four focused tests passed; real model generation succeeded on both plain-title orders in 6 and 5 seconds. Each generated recipe executed 3,600 ticks with food collection, 120 shots and 3 alien hits; replay produced identical points/hits. These are local generated-runtime checks, not a newly repeated multiplayer browser test. Broad QUALITY.md merge gates remain outstanding.


## Cached golden demo — 13 September 2026

User decision: make the verified Snake + Space Invaders demo load quickly; defer HTML/CSS/asset remix expansion. Exactly two initial cards containing these titles (either order, ignoring case/whitespace) reuse the retained output of a real `gpt-6-astra` request. The cache keeps that response/model identity and original build hash, binds the interpretations to the new room's actual cards, and labels the result “Cached AI-generated demo.” It never claims a fresh model call. Custom modifiers, different references, a third instruction, and later remixes continue through live generation. The retained engine, room clock, seed, input capture, scoring and archive contracts remain unchanged; generated origin gains optional explicit reuse provenance.

Fresh checks: six focused cache/generation-policy tests passed, plus TypeScript, lint and the production Worker build. The cached resolver and a full 3,600-tick replay completed in 41 ms / 17 ms for the two card orders without database/model access. A local production-Worker test using two isolated Chrome sessions loaded the cached game in 61 ms from host click to readiness, completed the shared 60-second round, and showed the same authoritative results in both browsers with zero browser errors: both 45 points, host 19 hits/rank 1 and friend 20 hits/rank 2. This is local two-browser evidence, not a production latency guarantee or hosted multi-device test. The same room protocol starts fresh AI-generated builds concurrently too. Full QUALITY.md merge gates and human review remain outstanding as previously documented.

Cut/rollback: restore the preceding Sites publication or remove the exact-pair cache branch; live generation and all retained builds remain usable. Cache source and matching logic are confined to `lib/party-forge/server/cached-*`, `demo-match.ts`, generation orchestration, additive origin metadata, and the three existing instruction/summary/lobby components. Ownership, approved PC-04 base and deferred PC-09 scope above remain unchanged.


## Cache aliases and unsupported-request feedback — 13 September 2026

User decision: cache matching ignores capitalization, card order, singular/plural forms and punctuation. Snake/snakes and Space Invader/Space Invaders reuse the saved mesh; a third player repeating either reference also uses it, with each actual card retaining its own interpretation. Additional instruction words still use normal generation rather than being discarded. These matching rules supersede the preceding strict two-card/third-card cache restriction; later remix rounds remain live-generation requests.

Audio/music/sound/rhythm requests and Tap Tap Revenge / Dance Dance Revolution references (including taptap and the supplied “revlution” spelling) now prompt a rewrite directly in the editor and are independently rejected by the server before model/job access. This is an explicit local capability check, not a model response, and consumes no generation attempt. Other unsupported mechanics remain subject to the existing model/schema validation.

Checks: eight focused tests passed, including case/order/plural aliases, preserved per-card interpretation bindings, third-player repeat, new-term generation routing, and nine blocked audio/rhythm examples without job access. TypeScript, lint and production build passed. A local two-browser check blocked the misspelled rhythm reference immediately, then loaded reversed “space invader” / “SNAKES” in 106 ms with no browser errors. The full concurrent 60-second test above remains the gameplay evidence; this subsequent check did not repeat unchanged gameplay. Existing packet ownership, scope, rollback and outstanding merge gates remain as above.


## Stable live play, explicit lives and final results — 13 September 2026

User-reported evidence: the production screenshot showed the clock-interruption abort. The client recalculated its clock from heartbeat timestamps and invalidated runs on shifts over 500 ms; normal delayed responses could trigger that check. Production logs also contained slow room commands and revision rejections. Pixel clients now use response-time server timestamps, retain a monotonic anchor during play, and receive a six-second shared start lead. Simultaneous Ready clicks retry one known stale-revision rejection for the unchanged build; uncertain mutations retain their existing identity. Legacy clock and start behavior are preserved.

User decision: remove demo and implementation notes from the main game UI, use three lives with a 60-second maximum, compare final scores, celebrate the winner, and offer Replay/New mesh to both players. New generated and cached games use retained pixel-arcade-v2: each collision removes a life, and zero lives freezes movement, spawning and scoring. The fixed 60-second capture/replay protocol continues for authoritative results; the UI explicitly says results arrive when the round timer ends. The v1 executable, rules and manifest loader remain unchanged for saved/active older builds. Start a fresh room for the new three-life rules; existing frozen artifacts are not silently rewritten. Original model/card provenance stays in the manifest despite removing front-facing demo notes. Direction-pad contrast is corrected.

Final results compare every participant's authoritative points and hits. Rank-one participants receive a brief CSS confetti animation, suppressed for reduced-motion preferences; tied leaders are labeled honestly. Either current participant may prepare a replay of the same frozen build with a fresh round/seed and new readiness acknowledgments. New mesh returns to fresh-room creation. Existing cumulative Continue/end/archive actions remain available. No new assets, dependencies, arbitrary code, accounts or hosting migration.

Fresh verification: 19 focused cache, generation-policy, client, clock/replay and three-life tests passed; 18 legacy room reducer tests passed. One existing reducer test expected obsolete “fewer than three” wording although the committed implementation already said “fewer than the required participants”; only that stale message assertion was updated. TypeScript, lint and production build passed. Runtime tests cover three-life collisions in Snake/Invaders/Bounce, frozen score/state after game over, the 60-second maximum and byte-identical availability of v1. A local production-Worker run used two isolated Chrome sessions, simultaneous Ready clicks, and alternating 1.2/2.6-second response delays. Both played and stopped at three hits, showed identical 35–25 results, displayed confetti only for the winner, exposed both replay/new-mesh actions, and successfully prepared both browsers when the non-host clicked Replay. No browser errors. This is local evidence, not a hosted two-device run or a complete QUALITY.md merge pass.

File scope adds pixel-arcade-v2, result presentation/styles and focused tests to the existing room/client/controller/server files. PC-05 owner Kahhow, implementer Codex, shared-contract reviewer Lance and approved PC-04 base remain unchanged. Full independent review and merge gates remain pending. Rollback is the preceding Sites publication; retained v1/v2 bytes must remain available after any later forward deployment with saved v2 games.


## PC-05 early elimination release — 2026-09-13

Observed production round was aborted after 54.946 seconds, before the former 60-second submission deadline. Eliminated clients previously froze without submitting. Clients now submit the actual captured trace at zero lives. The server replays it, requires exactly three hits and a terminal state, and enforces elapsed play time. No input padding is introduced. Both verified submissions end the round immediately; a remaining player can continue until elimination or the 60-second maximum. Simultaneous revision rejections retry the same captured attempt. Existing v1/v2 executable bytes and provenance are preserved.

Validation: 22 focused behavioral tests passed (early finish, clock, lives, room); TypeScript, lint and production build passed. Two isolated browser sessions with delayed polling reached results 15.365 seconds after Start, displayed both 35-point scores and the tie, and allowed non-host Replay to prepare both browsers. No browser errors. First test exposed an early-trace stale-revision retry bug; the corrected rerun passed. Full QUALITY merge gates and independent review remain outstanding; this is a scoped Site release, not a merge readiness claim.

Owner: Kahhow. Implementer: Codex. Reviewer: Lance (pending). Packet PC-05, issue https://github.com/ghostleek/chaotic-forge/issues/34; approved base PC-04 PR https://github.com/ghostleek/chaotic-forge/pull/46 at 1dbccea77d4d2707328c704f1d20be8aabc3b2ac. Changed files: trial controller/view, trial capture/schema, score validation/reducer and early-finish test; these shared files are owned by this scoped implementation. No new dependency. Timebox: focused bugfix and release. Rollback: republish the previous Site version 12.
