# PC-08 — Independently verify the online demo and freeze its release

**Status:** Local issue draft; no GitHub issue/PR created, verified, or assigned.
**Proposed accountable owner / implementer:** Lance. **Experience acceptance and independent reviewer:** Kahhow. If either coauthors a fix under review, use another independent final reviewer.

## Outcome

A specific release candidate passes the complete three-browser journey and preserves the prior lab. The team has reproducible evidence of what actually works, a recovery path, and an honest demonstration of authored mashups.

Read [PRD](../../PRD.md), [PLAN](../../PLAN.md), [QUALITY](../../QUALITY.md), [AGENTS](../../AGENTS.md), every core packet's accepted result, and [prior review](../../archive/2026-09-13-mechanic-lab/mechanic-forge-pr-review.md) on dependency closure, review cost, and stale release evidence.

## Scope and file ownership

Allowed: `tests/browser/party-forge.spec.ts`, `tests/integration/party-release.*`, `docs/party-forge-runbook.md`, `docs/party-forge-acceptance.md`, and `README.md`. Lance owns test/release evidence; README edits coordinate with its current owner. Product fixes return to the owning packet with explicit paths and a fresh review. Do not use a broad release wildcard to alter runtime, UI, or schemas without review.

Record the exact candidate SHA, environment, hosting audience, artifact/runtime versions, test commands, outcomes, unresolved limitations, and actual URL where verified. Exercise the existing Sites/Vinext/Cloudflare/D1 path. Use separate browsers/contexts with independent capabilities, not seeded success reports. Check blur never pauses scoring; network recovery accepts retained traces only within original deadline plus transport grace. A lost trace stays incomplete, and whole-round retry requires a new ID.

Cover initial supported card choices, real FPS/zombie/cooking interaction, 60-second trials, both eligible additions, middle-player denial, alternating edit order, ties, DNF, caps/Pass, reconnection, consensus end, last-played save, future play, and new remix. Also verify trusted 30-second host grace, deterministic succession, missing-editor abort history, no transferred edit entitlement, and waiting/archiving when fewer than three remain. A replacement can enter only a new roster at the documented round boundary. Check every supported initial combination in behavioral tests; perform representative live human play in addition. Verify old saved executable versions remain available after the candidate deploy.

No live-generation claim belongs in the preset demo. PC-09 remains a distinct required target milestone; AR/gesture and adaptive difficulty remain later work. Evidence from local fixtures, old imports, or a prior deployment must retain its actual origin.

## Dependencies and execution

Hard dependency: PC-07 and all its accepted ancestors. Optional recorded fallback capture; it supplements a functioning release and cannot replace online acceptance. Before dispatch, record exact candidate/parent SHAs and actual issue/PR links; planning SHA `c39ddb8a7b428585e2ab0e6c59fe0b670c2803c7` is not a release base. Use `codex/pc-08-release-verification`. No feature merges after freeze; a necessary fix invalidates affected checks and needs revalidation before a new candidate is accepted.

Select browser/verification skills for full-flow checks, existing Sites hosting guidance for an authorized deployment and actual URL smoke, and framework/runtime skills only for fixes that require them. Do not deploy or publish merely because this issue draft exists. No alternate hosting provider is selected.

## Acceptance and planned validation

Run the exact gates: `npm run lint`, `npm run build`, `npm test`, independent adversarial review of the complete integrated diff, then a second lint/build/focused-test run after review fixes. Also run the built-Worker/D1 suite defined by PC-01, real three-participant smoke on the actual host, and old-artifact future-play verification. Record failures and exclusions; no passes are implied by this draft.

Human acceptance asks whether everyone understands why the game changed, whether each contribution matters, and whether they choose another round or replay after the reveal. Treat these as observed session feedback with participant count, not generalized proof of fairness or retention.

**Timebox:** 8–12 implementation/acceptance hours plus reserved cross-review budget. **Cut/rollback:** freeze early; cut optional presentation, never required online/save behavior. Restore the last verified deployment if needed while preserving compatible database records/migrations and retained runtimes. Report blocked host access honestly; do not call a local-only pass a public release.
