# PC-01 — Freeze the shared contract and prove the existing host

**Status:** Published as [GitHub issue #30](https://github.com/ghostleek/chaotic-forge/issues/30), assigned to @Leoendithas. No implementation PR created.
**Accountable owner / implementer:** Lance (@Leoendithas). **Reviewer:** Kahhow (@ghostleek), independent of implementation.

## Outcome

Both implementation lanes have one versioned protocol, and a clean build can run a minimal durable room on the existing Sites/Vinext/Cloudflare target. This is the mandatory foundation for own-browser multiplayer; a memory-backed demonstration does not satisfy it.

Read [PRD](../../PRD.md), [PLAN](../../PLAN.md), [QUALITY](../../QUALITY.md), [AGENTS](../../AGENTS.md), and the relevant shared-state/build findings in [prior review](../../archive/2026-09-13-mechanic-lab/mechanic-forge-pr-review.md). Existing G1–G4 behavior remains covered.

## Scope and file ownership

Allowed: `lib/party-forge/contracts.ts`, `tests/fixtures/party-forge.*`, `tests/party-contract.test.mjs`, `db/index.ts`, `db/schema.ts`, `drizzle.config.ts`, `drizzle/**`, `cloudflare-env.d.ts`, `scripts/party-host-*`, `playwright.party.config.ts`, `tests/integration/party-host.*`, `package.json`, `package-lock.json`, `tsconfig.json`, `.oxlintrc.json`, `vite.config.ts`, `next.config.ts`, `playwright.config.ts`, `.openai/hosting.json`, and `docs/party-forge-host.md`. Lance owns these shared files. `QUALITY.md` may update its description of the test environment if the runner changes, while preserving every exact gate. Do not rewrite app screens or import ModeShift into production.

Define typed concept cards, resolved build manifest, immutable runtime/asset versions, room snapshots/commands/receipts, trial input/result envelopes, contribution history, and archives. A manifest identifies preset versus live-generated origin and incompatibility/failure states. Freeze the first-demo three-player rules from the PRD. Test fixtures remain under tests.

The archival sync excludes `archive/**` from application TypeScript and lint checks; preserve that boundary. Declare logical D1 `DB`, retain the existing Site identity, generate inspected Drizzle migrations, and use a narrow server binding helper. Resolve Next-script versus Worker-host mismatch explicitly; preserve the golden behavioral suite. No alternate host or automatic memory fallback.

## Dependencies and execution

No packet dependency. Planning SHA `c39ddb8a7b428585e2ab0e6c59fe0b670c2803c7` is not dispatch-ready: reconcile dirty guidance/imports and record an accepted clean full SHA first. Use `codex/pc-01-contract-host`; actual PR/base links must be recorded before dispatch. Stacked work is user-approved, but merges serialize and children rebase after parent merges.

Select `vercel:nextjs` for framework/build boundaries and read the installed Next guides; `sites:sites-building` plus its persistence/SQLite references for the existing host, and `sites:sites-hosting` only for an authorized host proof. Browser verification skills apply when starting/testing the server. Do not assume those skills or tools exist in a remote implementer's environment; the acceptance below is authoritative.

## Acceptance and planned validation

Prove a versioned manifest/command schema rejects invalid data. Prove the built Worker emits its entrypoint, static assets, hosting metadata, and migrations. Apply a real local D1 migration; create/read a record; restart and reread it; race two expected-revision writes and accept exactly one. Confirm current host audience permits intended participants. Record unavailable credentials/capabilities as blockers.

Run `npm run lint`, `npm run build`, `npm test`, independent adversarial complete-diff review, then a second lint/build/focused-test run after fixes. Add documented Worker build, migration, and integration commands; these do not exist as repository scripts yet. No checks are claimed run by this draft.

**Timebox:** 8–12 implementation hours; review/gates reserved separately. **Cut/rollback:** defer presentation and optional schema fields; never waive durable host proof. Revert this isolated foundation if it breaks G1–G4. Failed hosting blocks dependent room acceptance while pure UI/domain planning can continue.
