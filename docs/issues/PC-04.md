# PC-04 — Shared room cards for one deterministic demo

[GitHub issue #33](https://github.com/ghostleek/chaotic-forge/issues/33). Remote issue text has not been reconciled: connector write permission was denied. This document records the user's latest accepted scope.

Human accountable owner: Kahhow (@ghostleek). Implementer: Codex with Kahhow. Human reviewer: Lance (@leoendithas), acceptance pending.

## Observable outcome

**Cap: three people total — the host plus two connected devices.** Further tests were stopped at the user’s explicit request; retain draft status.

Three people open an invitation in separate browsers, claim one available card each, confirm against the room authority, and see the same recipe and build. Cat waiting states reflect accepted contributions only.

## User decision: deterministic authored demo

Exactly three starting cards: Knockback, Pursuers, Quick orders. Anyone may claim an available card. The resulting recipe is fixed. No permutations, mixed hands, swapping, redraws, custom instructions or catalog browser. Later editor turns add Dinner bell, Hot potato, then Zombie pantry; Pass only after exhaustion. Existing winner/loser order and permissions remain authoritative. These cumulative additions are not controlled experiments.

## Implementation boundary

Client routes `/party` and `/party/[roomId]`, `lib/party-forge/client/**`, `components/party-forge/**`, reusable SVG assets, scoped design documentation and client/UI tests. Kahhow owns these files. Lance retains ownership of API/contracts/runtime/schema/package/hosting configuration; those are unchanged. Preserve Source, Forge interpretation, User decision, exact retained build identity and legacy G1–G4 behavior.

Base: merged [PC-03 PR #44](https://github.com/ghostleek/chaotic-forge/pull/44), commit `e529e74c7cbff5e63db7184734f553ae559bb27d`, on `codex/pc-04-room-cards`. PC-01 and PC-02 are included in that base. Parallel avatar work is reused; gesture research is excluded. Revalidate after parent changes and serialize merges.

The interface uses tab-scoped capabilities, versioned requests, durable command IDs and monotonic snapshots. Unknown outcomes retry the identical request. Rejected choices require a new explicit action. Invite links contain only room IDs. Polling is canceled on unmount; accepted choices survive reconnect.

Keep the flat typography-heavy cards, fixed dimensional hero, cat avatars and dismissible onboarding. No 3D toggle or permanent explainer/download sections. Details do not select a card. Hero interaction is decorative and reversible.

## Integration boundary and acceptance

PC-04 loads and validates the exact retained executable before acknowledging a build. It does not start a timed trial or simulate player scores. [PC-05 #34](https://github.com/ghostleek/chaotic-forge/issues/34) must integrate the playable viewport before the complete human demo can run. Later phase controls consume actual server snapshots/results; fixtures are test evidence only.

Acceptance: actual create/join, server-confirmed cards, lost-response retry, stale claims, reload recovery, honest connection/permissions, fixed additions/exhaustion, exact-build loading, keyboard/mobile behavior. The full end-to-end human gameplay acceptance remains dependent on PC-05.

Run `npm run lint`, `npm run build`, `npm test`, `npm run test:party`, independent complete-diff review, and second lint/build/focused tests after fixes. Run `node tests/fixtures/party-ui/live-check.mjs` for local three-browser UI acceptance. Record actual outcomes in `docs/design/pc-04-verification.md`; historical evidence does not count as a fresh pass.

Skills: Next.js and installed guides for routes/client boundaries; React guidance for components; browser verification with Playwright/Chrome as the available equivalent. Reuse accepted SVG/CSS assets; no image generation or deployment required.

Timebox: this implementation/review pass. Cut decoration first; never cut authoritative confirmation, recovery or provenance. Roll back scoped routes/client/components together without changing the accepted backend. No merge or deployment authorization. Keep the PR draft if gates or integration acceptance remain incomplete.
