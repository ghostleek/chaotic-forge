# PC-12: final-growth running animation and score beam

User decision: animate the second-growth Dino's legs and enable a dramatic two-second beam at score 1,000. Forge interpretation: automatic activation on the first score threshold crossing, once per run, at any growth stage. Clears visible forward spikes, walkers and meat without awarding collection/stomp points. Offscreen objects and those behind the muzzle remain. Pausing freezes the timer; restart clears the trigger and count. Existing score progression can cross the threshold through time, stomps or meat. A terminal collision is not retroactively undone.

The final-growth sprite is unchanged on disk. Its upper body and two leg regions are articulated in the canvas renderer while grounded; the pose holds airborne. The beam uses a steady cyan/white glow and slow variation rather than a screen flash. Intro and gameplay disclosures explain the trigger.

Human accountable owner: Kahhow. Implementer: Codex. Independent reviewer: Codex review_dino; human acceptance Kahhow/Lance. Base: 6e55f041c75c8549b5d6ded094de2ae9be143e2c, deployed Site version 20. No new GitHub issue/PR verified. Allowed files: Dino optional state, progressive engine, renderer, demo/modal copy, focused tests and this handoff. Lance retains shared runtime ownership. V1/V2 defaults, retained executable bytes/hashes, multiplayer replay and score remain unchanged. No dependencies, API calls, migrations or new assets. Demo rules revision: dino-mario/4.

Skills: existing Sites building workflow; React best-practices for the two component edits; existing Playwright Worker harness as the agent-browser CLI fallback. Canvas animation reuses the accepted raster, so no image generation/editing call is needed. Timebox: current session. Cut/rollback: restore Site version 20 or revert this scoped patch without changing saved data.

Acceptance: grounded legs visibly alternate while the torso is stable; no running animation while airborne; threshold-crossing activates the beam, incoming obstacles are removed before damage, duration is exactly 120 fixed steps, pause/restart/one-shot semantics hold, and destroyed meat/stomps add no rewards. Check full browser golden flow and QUALITY.md lint/build/tests/review/post-review focused checks. Implementation is local; no new deployment requested in this follow-up.

Validation: lint/build passed; all 214 unit tests passed, including 29 focused Dino checks. Independent review found no blockers and independently repeated the focused suite. Full browser run passed 52 existing/beam checks; two new leg-pixel checks initially compared raw pixels too strictly. The corrected checks compare leg changes with the stationary body as a control for GPU/readback variation; both desktop/mobile cases pass. The separate Dino browser rerun also passed all 16 non-leg cases. Beam and both stride poses were visually inspected. No product code changed after the reviewed build; only the pixel assertion was corrected. No deployment performed.

## Follow-up: pterodactyls, flat sprites and focus handling

Latest user decision: replace walkers with pixel pterodactyls, alternating crawling and low flight toward the player; animate folded-wing ground steps and flight wing phases. Flatten the evolved Dino and pterodactyl art to match the original Dino. Fix unexpected pausing. Source: user-supplied pterodactyl flight reference. Forge interpretation: an original muted-purple four-phase flying/four-phase crawling sprite sheet; deterministic alternating encounters at ground level or 24 pixels above ground. The world scroll moves both modes toward the player and accelerates continuously. Shared collision rectangles use that altitude; both modes can be stomped and destroyed by the beam. No randomness or live model calls.

Focus loss now clears held/pulsed jump input without pausing. Manual pause, introduction opening and document becoming hidden still pause and preserve state; returning to a hidden tab requires explicit resume. This removes incidental canvas/window blur pause triggers without running the game in a background tab.

Demo revision now dino-mario/5. Additional allowed files: sprite loader, new public Dino/pterodactyl PNGs and focused browser tests. Existing assets are preserved for rollback. Sites building, imagegen and React review skills apply. The asset agent generated original variants with built-in imagegen (embedded metadata gpt-image 2.0); no reference pixels were copied. New asset prompts and final validation follow below. Base, owners, reviewer, preserved contracts and rollback remain as above.

Pterodactyl accepted output: `public/party-forge/dino/pterodactyl-v1.png`, 1774×887 RGBA. Four equal columns and two equal rows; the renderer uses fractional source coordinates supported by Canvas. Original imagegen prompt:

> ONE sheet of an original pterodactyl enemy, all frames facing LEFT. Each character is a completely SOLID FLAT MUTED PURPLE silhouette (#806494) with exactly one small square TRANSPARENT eye cutout. The silhouette has a long left-pointing beak, small crest, compact body, wings and feet. Absolutely NO outline, NO internal lines, NO shading, NO highlights, NO texture, NO gradients, NO secondary colors. All opaque pixels must be the SAME purple. Style is extremely coarse chunky square geometry like a very simple early arcade dinosaur, with only a few large square steps forming the shape. EXACTLY FOUR equal columns and TWO equal rows, eight identical cells on a 1024x512 transparent canvas. Each sprite remains within the central 70 percent of its cell width and height, leaving generous transparent margins on ALL sides. Body/chest anchor stays at the same relative position in each cell, consistent scale. Top row: wings UP, HORIZONTAL, DOWN, HORIZONTAL recovery. Bottom row: wings FOLDED, left leg forward, gathered stance, right leg forward, gathered recovery. Genuine transparent alpha, including eye holes. No text, labels, numbers, logo, watermark, shadow, scenery, border, outline, details or extra objects.

Generated art has slight color variation despite the prompt and no mathematically exact cell-edge padding. This is an original simplified visual adaptation, not a traced copy of the supplied orange animation. An earlier detailed pterodactyl and a failed glowing Dino edit were discarded and are not shipped.

Final Dino implementation: the imagegen edits and fresh generation retained unwanted glow and were rejected. The shipped final-growth Dino instead uses original native Canvas block geometry, the same rendering method as the small Dino, with one solid green fill, cream square eye, stepped back spines and independently articulated legs. No failed generated Dino is loaded or shipped, and no existing raster was postprocessed. The accepted evolved-v1 PNG remains on disk only for rollback. The loader now requires meat and pterodactyl only. The static introduction shares this same evolved drawing.

Independent review of the complete code found a focus/pulse sequencing defect: keyboard Jump set its pulse before focusing the canvas, whose blur handler cleared the pulse. Both pointer and keyboard Jump handlers now focus before assigning their input. Reviewer repeated all31 focused Dino checks and reported no remaining code blockers. Browser checks explicitly cover visible canvas/window blur continuing the run, hidden-tab pause preserving ticks, Tab/Enter Jump, flight/crawl screenshots, beam pause and restart, and final-form leg movement. The initial non-escalated all-unit run passed215/216; its one failure was localhost listen EPERM in the cross-runtime test. The final permitted local-Worker run supersedes that environment failure.

Final validation (15 September2026): lint, production build and diff whitespace check pass. The permitted full npm test run passed216/216 unit tests and54/56 browser checks; both failures were the newly added pause test's exact60-frame assumption (actual59 at the sampled timer boundary). Corrected that test to assert progress, capture the actual frame, require exact preservation while hidden, then require progress after resume. The targeted rerun passes both desktop/mobile cases (2/2). No product code changed after the production build. Independent final code review reported no blockers and repeated31/31 Dino tests. Visually inspected screenshots `outputs/dino-pterodactyl/mode-276.png` (crawler), `mode-580.png` (flight and flat giant), plus the completed beam/leg playthrough. These are local Worker evidence only. No deployment performed; production remains Site version20 until requested.
